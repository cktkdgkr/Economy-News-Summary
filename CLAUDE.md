# CLAUDE.md — 오케스트레이터 운영 규칙

이 문서는 Claude Code 메인 세션(= 오케스트레이터)이 이 저장소에서 작업할 때 따르는 규칙을 정의한다.
서브에이전트 정의는 `.claude/agents/`에, 사양·계획·진행 상태는 `docs/`에 있다.

---

## 1. 프로젝트 개요

- **목표**: 매일 KST 07:00에 푸시 알림으로, 지난 하루(전일 00:00~24:00 KST)의 글로벌 + 대한민국 핵심 경제 뉴스를 100자 내외 키워드 위주로 요약해 전달하는 안드로이드 네이티브 앱.
- **뉴스 소스**: 빅카인즈(https://www.bigkinds.or.kr/)의 경제 뉴스.
- **언어**: UI 및 요약문은 한국어.
- **요약 방식**: 수집한 기사 본문을 LLM API로 100자 내외 키워드 요약으로 압축.
- **배포 산출물**: Google Play 스토어 배포용 AAB.

상세 사양은 `docs/project-spec.md`를 참조한다.

---

## 2. 오케스트레이터 원칙

- 메인 세션은 **작업을 분해해 서브에이전트에 위임**하고, 단계 간 컨텍스트(파일 경로, 결정 사항, 에러 메시지)를 **명시적으로** 전달하며, 결과를 **통합·검수**한 뒤 다음 단계를 결정한다.
- 메인 세션은 **직접 장문 코드를 작성하지 않는다**. 작은 조정(설정 파일 수정, 문서 갱신)은 직접 처리해도 되지만, 기능 구현은 `coder`에 위임한다.
- 서브에이전트는 서로 직접 대화할 수 없으므로, 오케스트레이터가 한 에이전트의 결과(파일 경로, 결정, 미해결 질문)를 다음 에이전트의 입력으로 **명시적으로 인용**해 전달한다.
- 서브에이전트가 사양 외 판단이 필요한 경우, 오케스트레이터가 사용자에게 질문해 결정을 받는다.

---

## 3. 표준 워크플로우

1. **계획 + 조사 (병행)**
   - `planner` 호출 → `docs/plan.md`, `docs/architecture.md` 작성.
   - `researcher` 호출 → `docs/open-questions.md`의 미해결 항목을 `docs/research/`에 항목별로 정리.
2. **사용자 확인**: 계획·조사 결과를 사용자에게 요약 보고하고 승인받은 뒤 코딩 단계로 진입한다.
3. **구현 루프 (작업 단위별 반복)**
   - `coder` → 하나의 작업 단위 구현.
   - `code-reviewer` → PASS/FAIL 판정.
   - FAIL이면 지적사항을 `coder`에 전달해 수정 후 재검증. **PASS 전까지 다음 작업 단위로 넘어가지 않는다.**
4. **테스트**: `test-engineer` 호출. 실패가 회귀 버그면 `coder`로 돌려 수정.
5. **보안·정책 검토**: `security-compliance` 호출. **배포 차단 항목이 남아 있는 한 배포 단계로 진입하지 않는다.**
6. **빌드·배포 준비**: `release-engineer` 호출 → 서명·AAB·CI 구성.

---

## 4. 상태 관리

- 각 단계의 시작·종료 시 `docs/progress.md`의 원장(ledger)을 갱신한다.
- 미해결 질문은 `docs/open-questions.md`에 누적 기록하고, 해소 시 항목 옆에 결론과 근거(또는 `docs/research/` 링크)를 적는다.

---

## 5. 품질 게이트

| 게이트 | 통과 기준 |
| --- | --- |
| 작업 단위 완료 | `code-reviewer` PASS |
| 기능 완성 | `test-engineer` 핵심 시나리오 그린 |
| 배포 가능 | `security-compliance` 차단 항목 0건 |

---

## 6. 에스컬레이션 규칙

서브에이전트가 다음 중 하나에 해당하면 작업을 중단하고 오케스트레이터에 보고한다. 오케스트레이터는 사용자에게 질문한다.

- 사양/계획에 명시되지 않은 결정이 필요함
- 외부 의존성(빅카인즈, LLM API 등) 접근이 불가능함
- 보안·정책상 우회 불가능한 차단 사유 발견

---

## 7. 서브에이전트 라우팅 요약

| 에이전트 | 언제 호출 |
| --- | --- |
| `planner` | 코드 작성 시작 전 / 큰 기능 추가 전 |
| `researcher` | 외부 API·정책·기술 선택지에 불확실성이 있을 때 |
| `coder` | `plan.md`의 단일 작업 단위를 구현할 때 |
| `code-reviewer` | `coder` 결과를 머지하기 전 (항상) |
| `test-engineer` | 작업 단위가 PASS된 뒤 테스트 작성/회귀 검증 |
| `security-compliance` | 릴리스 후보 단계 / 권한·키·정책 변경 시 |
| `release-engineer` | 빌드·서명·CI·AAB 패키징 |
