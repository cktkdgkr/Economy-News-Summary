{
  "name": "economy-news-summary-functions",
  "version": "0.1.0",
  "description": "Economy News Summary — Firebase Cloud Functions (Node.js 20 + TypeScript)",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.6.0"
  },
  "devDependencies": {
    "typescript-eslint": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.0.0"
  }
}
